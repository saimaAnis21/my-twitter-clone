
<section class='section'>
  <div class='container'>
    <div class='columns'>
       <%= render 'trends' %>
       <%= render 'feed' %>
       <%= render 'whot-to-follow' %>
    </div>
  </div>
</section>

